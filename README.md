# PassKeeper for barroit

### init dependencies

```bash
./run_build init
```

### build for linux

```bash
./run_build
# or
./run_build penguin
```

### build for windows

```bash
./run_build window
```

### cleanup build

```bash
./run_build clean
```

